# Ruff configuration for the backend + tests (CI runs `ruff check backend tests`)
target-version = "py311"
line-length = 120

[lint]
# Default rule set: pycodestyle errors + pyflakes
select = ["E4", "E7", "E9", "F"]
